<center>
<br>
<br>
<div class="row">
    <div class="col s2"></div>
    <div class="col s8">
        <br>
        <h3><%=@room.title %></h3>
        <hr>
        <br>
        <h5>:: 게임 설명</h5>
        <br>
        <div class="content_box">
        <p><%=@room.content %></p>
        </div>
        <br>

        <p>플레이 인원 수를 선택해주세요.</p>


        <% if @stages %>
            <% if @roomCal.nil? || @roomCal.mode==0 %> <!--대기중 -->
                <%= link_to '<i class="material-icons left">person</i>혼자 시작하기'.html_safe, room_cal_create_path(@room.id), method: :post, :class => 'waves-effect waves-light btn' %>
                <%= link_to '<i class="material-icons left">group</i>팀으로 시작하기'.html_safe, room_team_path(@room.id,current_user.id), method: :get, :class => 'waves-effect waves-light btn' %>
            <% elsif @roomCal.mode==1 %><!--싱글플레이 -->
                <%= link_to '<i class="material-icons left">person</i>혼자 계속하기'.html_safe, stage_show_path(@room.id), method: :post, :class => 'waves-effect waves-light btn' %>
            <% elsif @roomCal.mode==2 %><!--멀티플레이 -->
                <%= link_to '<i class="material-icons left">person</i>팀으로 계속하기'.html_safe, stage_show_path(@room.id), method: :post, :class => 'waves-effect waves-light btn' %>
            <%end%>
            
            <br><br>
            <br><br>
            <%= link_to '<i class="material-icons left">person</i>RoomCal초기화하기'.html_safe, room_cal_delete_path(@room.id), method: :post, :class => 'waves-effect waves-light btn' %>
        <%end%>
        
         <p>* 다인 플레이 선택 시, 방탈출 클리어 보너스로 획득하는 EP(Escape Point)가 감소됩니다.</p><br><br>
         
        <p>
          <strong>Like:</strong>
          <%= @room.likes.size %> 명이 좋아합니다. 
        </p>
     
    <p>
      
      <%=form_tag like_toggle_path, method: 'post' do %>
        <%=hidden_field_tag :room_id, @room.id %>
        <%=submit_tag ((current_user.is_like? (@room)) ? '좋아요 취소' : '좋아요')%>
      <% end %>
      
      
    </p>
        
        <br><br><hr><br><br><br><br>
    </div>
    <div class="col s2">

    </div> 
</div>
</center>